---
id: act-4-08
act: 4
title: "Pings"
kind: module
ap_topics: ["4.10"]
objectives:
  - "Determine whether a setting is binomial: binary outcome, independent trials, a fixed number of them, and the same probability on each"
  - "Calculate binomial probabilities P(X = k) and cumulative probabilities, keeping the boundary cell on the correct side of the inequality"
  - "Interpret a binomial probability in context, including the 10% condition when the trials are draws from a finite population"
prereqs: ["act-4-07"]
est_minutes: 30
calc_briefing: null
summary: "Ten minutes from the wings going out, the Eyes ask for a number instead of a reassurance. The window's close passes were on the public plot before the window opened — different hulls, different offices, nothing joining them — which is the one thing Nightjar's own re-sweeps never were."
---

import { binomial, binomialMoments, binomialRV, convolve, discreteRV, fmt, fmtInt, fmtPct, rvProb } from '@/lib/stats'
import {
  ARRIVALS_PER_DAY,
  CLOSE_PASSES,
  CLOSE_PASS_KM,
  PURGE_HOURS,
  PURGE_NAVAL_AU,
  P_PERRINE_PER_HOUR,
  P_SEEN_COLD,
  P_SEEN_PER_PASS_COLD,
  P_SEEN_PER_PASS_PURGING,
  WINDOW_1,
  WINDOW_2,
  heatFor,
  hoursBetween,
  metStamp,
  pNoDetection,
  pPerrineInWindow,
  perrineMoments,
  windowOnePasses,
} from '@/instruments/act-4/data'
import { BeingSeenSimulator } from '@/instruments/act-4/BeingSeenSimulator'
import { BinomialExplorer } from '@/instruments/act-4/BinomialExplorer'
import { KeyTable, SinkReadout, oneIn } from '@/instruments/act-4/_ui'
import { ProfileEcho } from '@/instruments/act-4/story'

export const heat = heatFor('act-4-08')
export const passes = windowOnePasses
/** The window's scheduled close passes: a fixed n, known before the window opened. */
export const nPasses = passes.length
export const windowHours = hoursBetween(WINDOW_1.from, WINDOW_1.to)
export const windowTwoHours = hoursBetween(WINDOW_2.from, WINDOW_2.to)
export const hourTrials = Math.round(windowHours)
export const perrineOnPassList = passes.filter((p) => p.owner_class === 'Perrine').length
/** Perrine passages of Mark 9 during window 1 (beat sheet): two, on profile, neither off it. */
export const perrineObserved = 2
export const passRows = passes.map((p) => [p.hull, metStamp(p.met), p.owner_class, `${fmt(p.range_km / 1e6, 2)} Mm`])
/** The beats' answers — computed here, never printed before the gate. */
export const pNone = pNoDetection(passes)
export const pPerrine = pPerrineInWindow(windowHours)
export const pSeenAtAll = binomial.atLeast(1, nPasses, P_SEEN_PER_PASS_COLD)
export const passMoments = binomialMoments(nPasses, P_SEEN_PER_PASS_COLD)
export const perrineExpected = perrineMoments(windowHours)
/** The worked example runs on the near point and on a single day — never on the beats' numbers. */
export const nearPasses = CLOSE_PASSES.near
export const nearNone = binomial.pmf(0, nearPasses, P_SEEN_PER_PASS_COLD)
export const nearOne = binomial.pmf(1, nearPasses, P_SEEN_PER_PASS_COLD)
export const nearAtMostOne = binomial.cdf(1, nearPasses, P_SEEN_PER_PASS_COLD)
export const nearFewerThanOne = binomial.cdf(0, nearPasses, P_SEEN_PER_PASS_COLD)
export const purgeWindowRV = convolve(binomialRV(nearPasses - 1, P_SEEN_PER_PASS_COLD), discreteRV([0, 1], [1 - P_SEEN_PER_PASS_PURGING, P_SEEN_PER_PASS_PURGING]))
/** One of the twelve passes catches her with the wings out: eleven cold trials plus one hot one. */
export const nearWithPurge = rvProb(purgeWindowRV, (v) => v >= 1)
export const dayHours = 24
export const dayNone = binomial.pmf(0, dayHours, P_PERRINE_PER_HOUR)
export const dayOne = binomial.pmf(1, dayHours, P_PERRINE_PER_HOUR)
export const dayAtLeastOne = binomial.atLeast(1, dayHours, P_PERRINE_PER_HOUR)
export const dayMoments = binomialMoments(dayHours, P_PERRINE_PER_HOUR)
export const pmfTex = `P(X = k) = \\binom{n}{k}\\,p^{k}(1-p)^{\\,n-k}, \\qquad k = 0, 1, \\ldots, n`
export const nearOneTex = `P(X = 1) = \\binom{${nearPasses}}{1}(${fmt(P_SEEN_PER_PASS_COLD, 2)})^{1}(${fmt(1 - P_SEEN_PER_PASS_COLD, 2)})^{${nearPasses - 1}} = ${fmt(nearOne, 4)}`
export const nearNoneTex = `P(X = 0) = (1 - ${fmt(P_SEEN_PER_PASS_COLD, 2)})^{${nearPasses}} = (${fmt(1 - P_SEEN_PER_PASS_COLD, 2)})^{${nearPasses}} = ${fmt(nearNone, 4)}`
export const nearBoundaryTex = `P(X \\le 1) = ${fmt(nearAtMostOne, 4)} \\qquad P(X < 1) = ${fmt(nearFewerThanOne, 4)} \\qquad \\text{difference} = P(X = 1) = ${fmt(nearOne, 4)}`
export const dayTex = `P(X \\ge 1) = 1 - P(X = 0) = 1 - (${fmt(1 - P_PERRINE_PER_HOUR, 4)})^{${dayHours}} = ${fmt(dayAtLeastOne, 4)}`

<SinkReadout sinkPct={heat.sinkPct} stamp={metStamp(heat.at)} profile="Watch · ten minutes to the wings" />

<Scene stamp={metStamp(heat.at)} location={`Sensor bay · Mark 9 region, 0.42 AU from Ceres · Watch, cellar ${fmt(heat.sinkPct, 0)}%`} condensed="Ten minutes from the end of the first Watch window, with the cellar at ninety-six percent, Ebele has the window's close passes on the plot — the same list he had before the window opened, because the Lane plot is public. Oyelaran will not accept 'effectively invisible' and makes him say why these passes are independent when his own sweeps were not. Two Perrine hulls went by on profile; nothing turned; the wings go out at 02:00.">

Sixty-two hours on Watch and the cellar reads {fmt(heat.sinkPct, 0)} percent, which is the number Sandoval said it would be within a point and a half. The bay smells of hot plastic. Nobody has said anything unnecessary since the second watch.

Ebele has the window's close-pass list up, and the thing worth noticing about it is that it is the *same list* he had at {metStamp(WINDOW_1.from)}, before the window opened, before any of it happened. The Lane plot is public. Every hull that filed a track carrying it inside {fmt(CLOSE_PASS_KM / 1e6, 1)} million kilometres of the holding point over the last {fmt(windowHours, 0)} hours was on that plot with a name, an owner and an hour against it.

<KeyTable
  columns={['hull', 'closest approach', 'owner class', 'range']}
  rows={passRows}
  caption={`The plot, filed in advance · ${fmtInt(nPasses)} scheduled close passes inside ${fmt(CLOSE_PASS_KM / 1e6, 1)} million km of the holding point, MET ${metStamp(WINDOW_1.from).slice(4)} to ${metStamp(WINDOW_1.to).slice(4)}`}
  ariaLabel="Scheduled close passes of the holding point during Watch window one"
/>

Four hulls. Four chances for somebody else's navigation set to resolve a cold corvette against the background, each one of them small — the Eyes price it at {fmtPct(P_SEEN_PER_PASS_COLD, 0)} a pass while cold — and each one of them the same size as the others, because the geometry at the holding point does not care which hull is flying it.

<Dialogue speaker="analyst" aside="He has the list and the per-pass figure on the same screen and he is pleased with both, which is his whole problem.">
So — four passes, sir, two in a hundred each, and we are cold the whole way through. That is effectively invisible. I would write *no detection* in the annex and move on to the purge.
</Dialogue>

Oyelaran turns her chair a quarter and looks at him until he stops.

<Dialogue speaker="sensors" aside="She has been on Watch for sixty-two hours and her voice has gone down rather than up.">
*Effectively* is not a number, Ensign. Write a number.
</Dialogue>

He starts to multiply, and then he stops himself, which is new.

"Not like the sweeps," he says. "The sweeps weren't — I can't cube anything here." He looks at the list again. "Or can I. Why can I."

"Say it."

"Because these are four different ships." He is working it out as he says it, which is the only way he ever learns anything. "*{passes[0].hull}* doesn't know *{passes[2].hull}* came past. They filed their tracks with two different offices weeks apart and neither office has ever heard of us. Nothing that happens on one pass changes what the next hull does, or where its aperture is pointed, or what it is looking for — which is nothing, because none of them is looking for anything. Whereas my sweep log —"

"Your sweep log," Oyelaran says, "is one aperture with doctrine behind it. It finds something, it goes back. That is a ship deciding. These are four masters hauling helium-3 past a rock they have no reason to examine."

<Dialogue speaker="sensors" aside="She says it once, flatly, the way she says everything she expects to be written down.">
Fixed number of passes, known in advance. Seen or not seen, nothing in between. Same chance on each, because the range band is the same. And no pass knows about any other pass. Four conditions, Ensign. When they hold, the count has a name and a formula, and when they do not, it has neither.
</Dialogue>

There is a second count on the same plot and it is the one you actually came here for. Perrine hulls arrive at Mark 9 at {fmt(ARRIVALS_PER_DAY.Perrine, 2)} a day, six years of the Ledger behind that figure. Cut the window into hours and each hour is a trial of its own with a small, equal chance of carrying a Perrine hull past you.

<Dialogue speaker="analyst" aside="Fast again, and reaching for the number he knows how to compute rather than the one he was asked for.">
So — {fmt(ARRIVALS_PER_DAY.Perrine, 2)} a day across {fmt(windowHours, 0)} hours is {fmt(perrineExpected.mean, 2)} hulls, sir. Call it one. We should expect one Perrine passage in the window and {fmtInt(perrineObserved)} came up the approach — {fmtInt(perrineOnPassList)} of them close enough to be on that list — so we are running ahead of the rate.
</Dialogue>

Expected is not the same word as probable, and *how likely was at least one* is a different question from *how many on average* — and it is the question the second window's plan will be written against. Both numbers are yours to produce before the wings go out.

</Scene>

<Briefing title="A fixed number of independent trials" topics={["4.10"]}>
<Briefing.Intuition>
A **binomial** setting is four conditions, and they are worth learning as four words, because the model dies the moment one of them fails.

- **B**inary. Each trial lands in one of two buckets: seen or not seen, queried or not queried. No third outcome, no "partial contact".
- **I**ndependent. One trial's result does not change the chance on the next. This is earned from the mechanism, never from the feel of the data.
- **N**umber fixed. $n$ is settled *before* the counting starts. If the count of trials is the thing you are recording — sweeps *until* a contact — that is a different distribution.
- **S**ame probability. One $p$, holding on every trial. Three loiter points with three per-pass probabilities are three settings, not one.

Then $X$, the number of successes, has

$$P(X = k) = \binom{n}{k}p^{k}(1-p)^{n-k}$$

and the shape of that formula is worth reading rather than memorising. $p^{k}(1-p)^{n-k}$ is the probability of **one particular arrangement** — these $k$ trials succeed and those $n-k$ do not. Because the trials are independent and share a $p$, every arrangement with $k$ successes is worth exactly the same, so the answer is that one value multiplied by $\binom{n}{k}$, the number of arrangements there are. Drop the binomial coefficient and you have answered a question about one specific set of passes rather than about *some* $k$ of them.

**Independence, here, is not an assumption of convenience.** Two hulls whose tracks were filed months apart by two offices that have never heard of this ship are independent because no mechanism joins them. Two consecutive entries in *Nightjar*'s own sweep log are not, because the ship's doctrine sends the aperture back to a sector that just produced a contact — the second sweep exists *because* of the first. Same arithmetic, two settings, and only one of them may be multiplied out. The difference is physical, and it is the whole reason the close-pass count has a binomial and the sweep count does not.

**Cumulative probabilities, and the cell on the boundary.** Most real questions are not about one count but about a range, and a range is a sum of cells:

$$P(X \le k) = \sum_{i=0}^{k} P(X = i)$$

On a discrete count, "at most $k$" and "fewer than $k$" differ by the entire cell at $k$ — not by a rounding error, by a real quantity of probability. The same distinction runs up the other end: "at least $k$" keeps the cell at $k$, "more than $k$" throws it away. Write the inequality in symbols before you compute, every time. Upper tails are best taken as $P(X \ge k) = 1 - P(X \le k-1)$ — the complement of everything strictly below the boundary, which is the form that keeps the boundary cell where it belongs.

**Arrivals as a binomial.** Traffic does not arrive in trials; it arrives in continuous time. Cut the window into hours, though, and each hour is very nearly a trial: either a Perrine hull passes in it or one does not, the rate is the same hour to hour, the number of hours is fixed by the plan, and one hour's traffic does not schedule the next hour's. It is a model laid over a process, and it is a good one while $p$ per hour stays small — which at {fmt(ARRIVALS_PER_DAY.Perrine, 2)} arrivals a day it does.

**The 10% condition.** When the trials are *draws without replacement* from a finite file — a hundred and forty certifications pulled from an archive — the trials are not strictly independent at all, because every draw changes what is left. The binomial survives it when the sample is no more than a tenth of the population: $n \le 0.10N$. Under that limit the probability drifts too little between draws to matter. Over it, the model misstates how far the count wanders, and the honest answer is that a binomial does not apply. The condition is a ratio; a sample size means nothing until you have named what it is a tenth of.
</Briefing.Intuition>
<Briefing.Definition>
<Formula tex={pmfTex} terms={{ n: 'the number of trials, fixed before counting', p: 'the probability of success on one trial, the same every time', k: 'the number of successes asked about', '\\binom{n}{k}': 'how many arrangements of k successes among n trials there are' }} label="The binomial probability of exactly k successes" />

<Formula tex="P(X \le k) = \sum_{i=0}^{k} \binom{n}{i}p^{i}(1-p)^{n-i} \qquad P(X \ge k) = 1 - P(X \le k-1)" terms={{ 'P(X \\le k)': 'at most k — the cell at k is included', 'P(X \\le k-1)': 'everything strictly below k, which is what an upper tail removes' }} label="Cumulative and upper-tail forms" />

<Formula tex="\mu_X = np \qquad \sigma_X = \sqrt{np(1-p)}" terms={{ '\\mu_X': 'the expected number of successes — an average over many repetitions of the window', '\\sigma_X': 'how far the count typically wanders from that average' }} label="Where a binomial count sits and how far it moves" />

<Formula tex="n \le 0.10\,N" terms={{ n: 'the number of draws taken', N: 'the size of the population drawn from' }} label="The 10% condition — when draws without replacement may be modelled as binomial" />
</Briefing.Definition>
<Briefing.Worked>
The near loiter point, priced for the second window — the point inside the traffic bundle, which is not the point this window was flown from.

{fmtInt(nearPasses)} scheduled close passes, {fmtPct(P_SEEN_PER_PASS_COLD, 0)} a pass, cold throughout. Four conditions: the passes were on the plot in advance, so the trial count $n$ was fixed before the window opened; each pass either resolves her or does not; the per-pass probability is the same because the range band is; and the hulls are unconnected. Binomial.

**Exactly one hull sees her.**

<Formula tex={nearOneTex} label="One detection among twelve independent passes" />

**None at all**, which is the count the plan is really written against. Zero successes is a single arrangement, so there is no coefficient to apply — only the probability that a pass misses her, raised to the number of passes:

<Formula tex={nearNoneTex} label="No hull resolves her across twelve independent passes" />

The complement is the risk term: at least one hull resolves her somewhere in the window with probability {fmt(P_SEEN_COLD.near, 4)} — about {oneIn(P_SEEN_COLD.near)}. Out at the far point, where the plot carries {fmtInt(CLOSE_PASSES.far)} scheduled pass, the same arithmetic gives {fmt(P_SEEN_COLD.far, 4)}: the whole of the exposure is the one pass.

**The boundary cell, written out**, because this is where the question is usually lost:

<Formula tex={nearBoundaryTex} label="At most one against fewer than one" />

"At most one" and "fewer than one" differ by {fmt(nearOne, 4)} — {fmtPct(nearOne, 1)} of the whole distribution, sitting in a single cell. Nothing about the arithmetic warns you which one the sentence meant.

**What one purge does to it.** Price eleven of the twelve passes cold and one of them with the wings out at {fmt(P_SEEN_PER_PASS_PURGING, 2)}, and the probability that at least one hull sees her goes from {fmt(P_SEEN_COLD.near, 4)} to {fmt(nearWithPurge, 4)}. One pass carries more risk than the other eleven together. A purge is {fmt(PURGE_HOURS, 1)} hours as a lamp to anything with a naval-grade aperture inside {fmt(PURGE_NAVAL_AU, 2)} AU, and the plan has to say so.

**The other binomial: arrivals.** Take a single day at Mark 9 — {fmtInt(dayHours)} hourly trials, each carrying a Perrine hull with probability {fmt(ARRIVALS_PER_DAY.Perrine, 2)} ÷ 24 = {fmt(P_PERRINE_PER_HOUR, 4)}.

<Formula tex={dayTex} label="At least one Perrine passage in a day, from the complement of none" />

Expected arrivals in the day: np = {fmt(dayMoments.mean, 3)}, with a standard deviation of {fmt(dayMoments.sd, 3)}. The two numbers do not say the same thing. The expected count is {fmt(dayMoments.mean, 2)}; the probability of seeing at least one is {fmt(dayAtLeastOne, 4)}. An average of {fmt(dayMoments.mean, 2)} hulls a day and a {fmtPct(dayAtLeastOne, 0)} chance of a hull today are different facts about the same distribution, and a plan that confuses them buys the wrong window.
</Briefing.Worked>
<Briefing.Caution>
*Multiplying without checking independence.* The binomial's whole licence is that the trials do not talk to each other. *Nightjar*'s consecutive sweep entries fail that test — a contact is what sends the aperture back — and a binomial laid over them makes runs look roughly twenty times rarer than they are. Before the formula, name the mechanism that would connect two trials, and then say why it is absent. "They seem unrelated" is not that sentence.

*Sampling without replacement, with no 10% check.* Pulling records from a file is not a sequence of independent trials: each draw changes the file. The binomial is adequate anyway when $n \le 0.10N$, and not otherwise. Quoting a sample size without naming the population it came out of leaves the condition unchecked — 140 draws are fine from an archive of 2,800 and not fine from an archive of 400.

*Reading $P(X \le k)$ for $P(X < k)$.* On a count these differ by the whole cell at $k$. "At most", "no more than" and "up to and including" keep it; "fewer than", "under" and "less than" throw it away. The same trap runs upward: "at least $k$" keeps the cell, "more than $k$" does not, which is why an upper tail is taken as $1 - P(X \le k-1)$ and not $1 - P(X \le k)$.

*Quoting the expected count as a probability.* $np$ is an average over many repetitions of the window. It is not the chance of seeing one. A window with an expected {fmt(dayMoments.mean, 2)} arrivals a day carries a {fmt(dayAtLeastOne, 4)} probability of at least one — neither number substitutes for the other, and only one of them belongs in a risk column.
</Briefing.Caution>
</Briefing>

<BeingSeenSimulator band="near" window={1} mode="binomial" seed="act-4-08" />

<BinomialExplorer n={Math.round(windowTwoHours)} p={P_PERRINE_PER_HOUR} k={2} mode="probability" label="an hour of the second Watch window" />

<Drill generator="act-4/binomial-pmf" count={1} />
<Drill generator="act-4/binomial-cumulative" count={1} />
<Drill generator="act-4/binomial-none" count={1} />
<Drill generator="act-4/binomial-at-least" count={1} />
<Drill generator="act-4/binomial-conditions" count={1} />
<Drill generator="act-4/ten-percent-condition" count={1} />

<MissionBeat id="act-4-08-no-detection" kind="numeric" label="THE WINDOW, UNOBSERVED"
  answer={pNone} answerKind="proportion" digits={4}
  prompt={`The plot had this window's close passes on it before the window opened: **${fmtInt(nPasses)}** scheduled passes inside ${fmt(CLOSE_PASS_KM / 1e6, 1)} million kilometres of the holding point, each one a different hull on a track filed by an office that has never heard of this ship. *Nightjar* was cold for every one of them, and the Eyes price a passing hull's nav sensors at **${fmt(P_SEEN_PER_PASS_COLD, 2)}** of resolving her on any single pass.\n\nWhat is the probability that the whole window passed with **no detection at all**? Report a probability to four decimal places.`}
  hint="Zero successes is one arrangement, so the binomial coefficient is 1 and the formula collapses to a single power. The factor that gets raised to it is the probability that a pass does NOT resolve her.">
  <Success>
  <Dialogue speaker="sensors" aside="She writes it on the window sheet herself, under the pass list, and initials the line.">
  That is a number. — And the one under it is the one the Chief reads: take it off one and you have what the window actually cost us in exposure, which is not zero and was never going to be.
  </Dialogue>
  </Success>
  <Failure>
  <Dialogue speaker="sensors" aside="She does not take the slate. She puts one finger on the per-pass column and leaves it there.">
  Four passes, Ensign, and each of them fails to see her ninety-eight times in a hundred. Not two in a hundred — ninety-eight. Nothing is added here; four independent things all have to go the same way, and things that all have to happen multiply. — The Eyes would like that number to be right.
  </Dialogue>
  </Failure>
</MissionBeat>

<MissionBeat id="act-4-08-perrine" kind="numeric" label="AT LEAST ONE PERRINE HULL"
  answer={pPerrine} answerKind="proportion" digits={4}
  prompt={`Now the count the window was bought for. Perrine hulls arrive at Mark 9 at **${fmt(ARRIVALS_PER_DAY.Perrine, 2)}** a day over six years of the Ledger. Cut the window into hours and treat each hour as an independent trial carrying a Perrine passage with probability ${fmt(ARRIVALS_PER_DAY.Perrine, 2)} ÷ 24.\n\nWindow one ran **${fmt(windowHours, 0)} hours**. What was the probability that **at least one** Perrine hull passed during it? Report a probability to four decimal places.`}
  hint="At least one is the complement of none. Work out the probability that all of the hourly trials come up empty, and take it off one — never the expected count, which is a different quantity answering a different question.">
  <Success>
  <Dialogue speaker="analyst" aside="He puts it beside the expected count, in the same size, which is what Ferrier has been trying to make him do for a fortnight.">
  So — two-thirds, sir, near enough. Which means a third of the time we fly a window this long and nothing Perrine-flagged comes past at all, and we would have spent sixty-two hours of cellar on an empty sky. — That is the number the second window's plan needs. Not the average. The odds.
  </Dialogue>
  </Success>
  <Failure>
  <Dialogue speaker="analyst" aside="Talking himself through it, which is how he checks himself now.">
  Not the average, sir. I keep reaching for the average. {fmt(perrineExpected.mean, 2)} hulls is what it comes to over many windows like this one — it is not a probability and it is not even on the same scale, because a probability cannot go above one and that number can. At least one is one minus none, and none is every hour of the window coming up empty.
  </Dialogue>
  </Failure>
</MissionBeat>

<Gated by={["act-4-08-no-detection", "act-4-08-perrine"]}>

<Scene stamp={metStamp(WINDOW_1.to)} location={`Sensor bay · purging, wings out, ${fmt(PURGE_HOURS, 1)} h`} condensed="The window closes with both numbers in the annex: the probability nobody saw her, and the probability there was anything to see. Two Perrine hulls passed on profile and neither turned. The wings go out at 02:00 and Solberg logs that Halden Reach is inside half an AU while the ship is a lamp — a cost nobody aboard can price yet.">

The wings go out at {metStamp(WINDOW_1.to).slice(4)} and the cellar starts to empty, and for {fmt(PURGE_HOURS, 1)} hours this ship is the brightest thing inside a quarter of an astronomical unit that is not a star.

Both numbers go in the annex above the pass list, with the conditions written out beside them, because Oyelaran will not sign a probability that does not carry the four lines that earned it.

*Window one, {fmt(windowHours, 0)} hours at the holding point. {fmtInt(nPasses)} scheduled close passes, cold throughout, {fmt(P_SEEN_PER_PASS_COLD, 2)} per pass. P(no detection) = {fmt(pNone, 4)}; P(at least one hull resolves her) = {fmt(pSeenAtAll, 4)}, which is {oneIn(pSeenAtAll)}. Expected detections {fmt(passMoments.mean, 3)}. Perrine arrivals modelled as {fmtInt(hourTrials)} hourly trials at {fmt(P_PERRINE_PER_HOUR, 4)}: P(at least one passage) = {fmt(pPerrine, 4)}, expected {fmt(perrineExpected.mean, 2)}. Observed: {fmtInt(perrineObserved)} Perrine passages of Mark 9, both on profile, neither off it; {fmtInt(perrineOnPassList)} of them also on the close-pass list.*

Two Perrine hulls came up the Lane in sixty-two hours and both of them flew the approach the way the approach is flown. Nothing faded. Nothing turned. The Eyes held each of them from acquisition to the far side and logged a vector you could set a clock by.

<ProfileEcho
  watch="The tracker had both of them the whole way, which is what the profile was bought for and what the cellar was spent on. If either had put its plume eleven degrees off, you would have the bearing, the range and the angle."
  quiet="Detection only, at the profile you ordered: two bearings, two fading transponders, and no vector on either. If one of them had turned you would have seen a plume stop and would not have been able to say whether it stopped or turned, which is the distinction Oyelaran named when you gave the order."
/>

<Dialogue speaker="analyst" aside="He is looking at the two numbers rather than at the result, which is the improvement.">
So — nothing happened, sir, and we can say exactly how surprised to be. Two hulls, both clean. The rate says we should expect about one in a window that long and we got two, and the odds of getting at least one were about two in three, so there is nothing in any of that to write home about. — I would have called two-out-of-two a pattern three weeks ago.
</Dialogue>

<Dialogue speaker="comms" aside="He logs the purge start, the purge end and the traffic state within the same minute, as he does every time." lag="lag 21 min 14 s · Valhalla">
Purge logged, sir, {metStamp(WINDOW_1.to).slice(4)}, wings out, {fmt(PURGE_HOURS, 1)} hours. Traffic state at the start: nothing inside detection range that I can see and nothing on the plot that should be. — Seen by nobody we can detect, sir. I will note that *Halden Reach* is inside half an astronomical unit and has been since the eighty-eighth.
</Dialogue>

Nobody answers that, because nobody aboard can price it. *Halden Reach* is a Service hull on a Service errand and there is no reason for her to be looking this way. What everybody in the bay understands, and nobody says, is that "seen by nobody we can detect" is a sentence with a denominator missing from it — and that {fmt(PURGE_NAVAL_AU, 2)} AU is a long way for a naval-grade aperture to be told to look at a lamp.

Sandoval wants the second window planned before the cellar is empty. Which means the next question is not about this window at all: it is about the Board's own arithmetic, and about thirty-one hulls in two thousand six hundred transits, and about which rate the Lane is entitled to be compared against.

</Scene>

<LogEntry
  title="Binomial: fixed n, the same p, independent trials, two outcomes"
  concept="A count is binomial when a fixed number of trials each land in one of two buckets, with the same success probability every time, and no trial carries information into another — then P(X = k) is the number of arrangements times the probability of one of them, and cumulative questions are sums of cells. Independence has to be earned from the mechanism: different hulls on tracks filed in advance by unconnected offices qualify, while a ship's own consecutive re-sweeps do not, because doctrine sends the aperture back to the sector that just produced a contact. When the trials are draws without replacement from a finite file the binomial is adequate only while the sample is at most a tenth of the population."
  formula="P(X = k) = \binom{n}{k}p^{k}(1-p)^{n-k} \qquad P(X \ge k) = 1 - P(X \le k-1) \qquad n \le 0.10N"
  mistake="Reading P(X ≤ k) for P(X < k). On a discrete count the two differ by the entire cell at k, which is a real quantity of probability and not a rounding difference — and the same trap runs upward, where “at least k” keeps the cell and “more than k” discards it. Its near relatives are dropping the binomial coefficient, and quoting the expected count np as though it were the probability of at least one."
  tags={['binomial', 'BINS', 'independence', 'cumulative probability', 'at most vs fewer than', '10% condition', 'close passes', '4.10']}
/>

</Gated>
