EXEMPLAR: If H₀ is true — Perrine-beneficiary hulls and every other hull are lost at the same rate — then a sample of transits on the Hundred-Day Lane would produce a difference in sample loss rates at least as extreme as the one observed with probability 0.0008.
---
RESP: If H₀ is true — Perrine-beneficiary hulls and every other hull are lost at the same rate — 
  correct= true score= 1 fb= Conclusion stated in context with all required elements.
---
RESP: If H0 is true and Perrine hulls are lost at the same rate as everyone else, then the proba 
  correct= true score= 1 fb= Conclusion stated in context with all required elements.
---
RESP: Assuming the null hypothesis is true, there is a 0.0008 probability of getting a differenc 
  correct= true score= 1 fb= Conclusion stated in context with all required elements.
---
RESP: There is a 0.0008 probability that H0 is true for transits on the Hundred-Day Lane and the 
  correct= false score= 0.5 fb= “The probability that H₀ is true” — The p-value is computed by ASSUMING H₀ and asking how often data this extreme would follow. It says nothing about how probable H₀ itself is — H₀ is either true or it is not, and no amount of data turns that into a probability.
---
RESP: There is a 0.0008 chance that the difference in the loss rate of transits on the Hundred-D 
  correct= false score= 0.5 fb= ““due to chance”” — This sentence quietly makes the p-value a probability about the world rather than about the data. Every sample differs from the null by chance; the p-value measures how often chance alone produces a difference at least this large, which is a different claim.
---
RESP: Assuming H0 is true, the probability of a loss rate difference at least as extreme as this 
  correct= false score= 0.5 fb= “The probability that H₀ is true” — The p-value is computed by ASSUMING H₀ and asking how often data this extreme would follow. It says nothing about how probable H₀ itself is — H₀ is either true or it is not, and no amount of data turns that into a probability. “1 − p as the probability of Hₐ” — 1 − p is not the probability that Hₐ is true, and it is not the probability the result is real. Both p and 1 − p are computed entirely under H₀. “A probability that Hₐ is true” — Nothing in this procedure produces a probability for a hypothesis — only a probability for data, computed under H₀.
---
RESP: The probability of getting exactly this loss rate for transits on the Hundred-Day Lane is  
  correct= false score= 0.6 fb= Missing: Conditions on H₀ being true (“assuming H₀ is true…”); “at least as extreme” as the observed statistic.
NORM sample: null : p1 equal p2 and alt : p1 greater p2; pc equal 0.0119
H EXEMPLAR: H₀: p₁ = p₂ and Hₐ: p₁ > p₂, where p₁ and p₂ are the true loss rate per transit for hulls whose policies name Perrine Holdings and hulls with every other beneficiary among transits on the Hundred-Day Lane, 2178 to the Register snapshot: the null says those two population proportions are equal, and the alternative says the first is greater than the second. The direction is fixed by the question and not by the audit, because the question the Board was asked is whether Perrine-beneficiary hulls are lost at a higher rate, and a lower rate would not be an accusation at all.
H grade exemplar: {"correct":true,"score":1,"feedback":"Conclusion stated in context with all required elements.","rubric":[{"label":"States H₀ as an equality (no difference)","met":true,"feedback":"H₀: p₁ = p₂ — the two groups share whatever the common rate is."},{"label":"States Hₐ with the direction the question implies (greater than)","met":true,"feedback":"Hₐ says the true loss rate per transit is greater than the other group’s."},{"label":"Hypotheses are about the parameter (true / population), not the sample","met":true,"feedback":"Say \"the true (population) proportion\". The sample proportion is already known; it is the evidence, not the claim."},{"label":"Justifies the direction from the question, not the data","met":true,"feedback":"The direction comes from the question: the question the Board was asked is whether Perrine-beneficiary hulls are lost at a higher rate, and a lower rate would not be an accusation at all."},{"label":"Names the context (what is measured, and for whom)","met":true,"feedback":"Say what and whom: the loss rate per transit of transits on the Hundred-Day Lane, 2178 to the Register snapshot."}]}
===== act-6/state-hypotheses 
PROMPT: Before the season opened, the Adrastea Lane Office wrote down that it expected the hulls carrying the older transponder set to drop transponder locks more often, because their sets are a generation older. The season's log: 24 of 170 and 35 of 300.

Which pair of hypotheses does this question call for? 
ANSWER: {"type":"choice","options":["$H_0:\\ p_1 = p_2$ and $H_a:\\ p_1 > p_2$ — where $p_1$ and $p_2$ are the **true** proportion of transits with a transponder dropout for hulls carrying the older transponder set and for hulls carrying the current set, respectively.","$H_0:\\ p_1 = 0.141$ and $H_a:\\ p_1 > 0.117$ — where $p_1$ and $p_2$ are the **true** proportion of transits with a transponder dropout for hulls carrying the older transponder set and for hulls carrying the current set, respectively.","$H_0:\\ p_1 = p_2$ and $H_a:\\ p_1 < p_2$ — where $p_1$ and $p_2$ are the **true** proportion of transits with a transponder dropout for hulls carrying the older transponder set and for hulls carrying the current set, respectively.","$H_0:\\ \\hat p_1 = \\hat p_2$ and $H_a:\\ \\hat p_1 > \\hat p_2$ — where $\\hat p_1 = 0.141$ and $\\hat p_2 = 0.117$."],"correct":0,"feedback":[null,"Both sample proportions have been substituted for the parameters. In a two-sample test the null is a *relation* between two unknowns — $p_1 = p_2$ — and it never names a number, because the common rate the two groups might share is not known either.","Wrong direction for this question. The Office wrote down before the season opened that it expected the older sets to drop locks more often, so the alternative was fixed before any data arrived.","$\\hat p_1 = 0.141$ and $\\hat p_2 = 0.117$ are already known, and 
SOLUTION: $$H_0:\ p_1 = p_2 \qquad H_a:\ p_1 > p_2$$

**$H_0:\ p_1 = p_2$ and $H_a:\ p_1 > p_2$ — where $p_1$ and $p_2$ are the **true** proportion of transits with a transponder dropout for hulls carrying the older transponder set and for hulls carrying the current set, respectively.**

$p_1$ is the true proportion of transits with a transponder dropout for hulls carrying the older transponder set; $p_2$ is the same parameter for hulls carrying the current set. The observed $\hat p_1 = 24/170 = 0.141$ and $\hat p_2 = 35/300 = 0.117$ are the evidence, not the claim.

The null names no number. That is the point of it: it says the two groups share *whatever* the common rate is, and that single statement is enough to build a sampling distribution for $\hat p_1 - \hat p_2$ — centred at 0, with a spread the two sample sizes fix.

The direction is greater than, because the Office wrote down before the season opened that it expected the older sets to drop locks more often, so the alternative was fixed before any data arrived.
===== act-6/hypotheses-in-context 
PROMPT: Before the season opened, the Adrastea Lane Office wrote down that it expected the hulls carrying the older transponder set to drop transponder locks more often, because their sets are a generation older. The season's log: 24 of 170 and 35 of 300.

State the hypotheses for the office's question — in symbols **and** in words, saying what each parameter is and whom it is about — and then give one sentence saying why the alternative points the way it does. Do not run any test. 
ANSWER: {"type":"interpretation","required":[{"label":"States H₀ as an equality (no difference)","phrasings":[{},{},{},{}],"polarity":"any","feedback":"H₀: p₁ = p₂ — the two groups share whatever the common rate is."},{"label":"States Hₐ with the direction the question implies (greater than)","phrasings":["greater","alt"],"polarity":"any","minMatches":2,"feedback":"Hₐ says the true proportion of transits with a transponder dropout is greater than the other group’s."},{"label":"Hypotheses are about the parameter (true / population), not the sample","phrasings":["true","population","parameter","all"],"polarity":"any","feedback":"Say \"the true (population) proportion\". The sample proportion is already known; it is the evidence, not the claim."},{"label":"Justifies the direction from the question, not the data","phrasings":["offic","wrot","less","befor","season","open","expect","older","set","lock","greater","often","alt","fix","data","arriv"],"minMatches":2,"polarity":"any","feedback":"The direction comes from the question: the Office wrote down before the season opened that it expected the older sets to drop locks more often, so the alternative was fixed before any data arrived."},{"label":"Names the context (what is measured, and for whom)","phrasings":["proportion","transit","transponder","dropout","all","adrastea","feeder","lane","hull","carry","older","set","current"],"minMatches": 
SOLUTION: **H₀: p₁ = p₂ and Hₐ: p₁ > p₂, where p₁ and p₂ are the true proportion of transits with a transponder dropout for hulls carrying the older transponder set and hulls carrying the current set among all transits on the Adrastea feeder lane: the null says those two population proportions are equal, and the alternative says the first is greater than the second. The direction is fixed by the question and not by the audit, because the Office wrote down before the season opened that it expected the older sets to drop locks more often, so the alternative was fixed before any data arrived.**

The sample proportions — $0.141$ against $0.117$ — do not appear anywhere in the hypotheses, and they should not. They are the evidence to be weighed against $H_0$, and a claim about them would be a claim about a fact already established.

Note also what the null does *not* say: it does not say the common rate is any particular number. It says only that the two groups share it. That single sentence is enough to build the sampling distribution of $\hat p_1 - \hat p_2$, which is all a test needs.
===== act-6/one-sided-or-two 
PROMPT: The Adrastea Lane Office posts an off-nominal advisory rate of 0.197 for the Adrastea feeder lane and has been told by three masters that the real figure is worse. The Office pulls 270 transits from the berth log and finds 47 that logged an advisory.

That is $\hat p = 0.174$. Taking $H_0:\ p = 0.197$, which alternative should the office test, and for which reason? 
ANSWER: {"type":"choice","options":["$H_a:\\ p < 0.197$ — because the audit came back at $\\hat p = 0.174$, which is below 0.197, so that is the side worth testing.","$H_a:\\ p > 0.197$ — because the masters complained that the true advisory rate is worse than the posted figure, so the Office is only interested in a rate above it.","$H_a:\\ p \\ne 0.174$ — the true rate differs from what this audit measured.","$H_a:\\ p \\ne 0.197$ — because a two-sided alternative is always the safer default."],"correct":1,"feedback":["This is the trap, and it is a trap even on the draws where it lands on the right symbol. The alternative is a statement about the **question**, decided before the data arrive. Choosing the side after seeing which way $\\hat p$ fell means you would have picked the other side had the sample fallen the other way — which is a two-sided test reported at a one-sided p-value, and it doubles the error rate you are advertising.",null,"Two faults at once. The comparison value is the posted figure 0.197, not the sample's own 0.174; and testing a sample against itself asks a question with no possible answer.","\"Two-sided by default\" is a reasonable instinct and the wrong answer here. The masters complained that the true advisory rate is worse than the posted figure, so the Office is only interested in a rate above it — a difference in the other direction would not change anything 
SOLUTION: $$H_a:\ p > 0.197$$

**$H_a:\ p > 0.197$ — because the masters complained that the true advisory rate is worse than the posted figure, so the Office is only interested in a rate above it.**

The alternative is part of the *plan*, and the plan is written before the data. Here the masters complained that the true advisory rate is worse than the posted figure, so the Office is only interested in a rate above it, so the alternative is one-sided for a reason that has nothing to do with this audit and would read the same if the count had come back at 223 instead of 47.

The seductive wrong answer is the one that reads the direction off $\hat p = 0.174$. Note what it commits you to: had the sample fallen the other way, you would have tested the other tail. A procedure that will reject on *either* tail is a two-sided procedure, and reporting it as one-sided understates its false-alarm rate by a factor of two.
===== act-6/pooled-proportion 
PROMPT: The Adrastea Lane Office suspects that hulls under the co-operative charter are being held up more often than the rest of the Adrastea feeder lane, and pulls both columns from the same six-year berth log: 41 of 190 against 42 of 280.

The office will test $H_0:\ p_1 = p_2$ against $H_a:\ p_1 > p_2$. Under that null the two groups share one common rate, and the best estimate of it uses **both** samples.

Compute the pooled proportion $\hat p_c$, to three decimal places. 
ANSWER: {"type":"numeric","value":0.17659574468085107,"kind":"proportion","digits":3} 
SOLUTION: $$\hat p_c = \frac{x_1 + x_2}{n_1 + n_2} = \frac{41 + 42}{190 + 280} = \frac{83}{470} = 0.1766$$

**0.177**

It is not the average of 0.216 and 0.150, which would be 0.183. The two samples are different sizes — 190 against 280 — so the larger one has more to say about the common rate, and pooling the raw counts weights them automatically.

This is a *null-hypothesis* estimate: it exists only because $H_0$ said the two groups share a rate. It is the number the test's standard error and the test's conditions both run on, and it is not used at all when the same two samples are turned into a confidence interval for the difference.
===== act-6/expected-count 
PROMPT: Before the season opened, the Adrastea Lane Office wrote down that it expected the hulls carrying the older transponder set to drop transponder locks more often, because their sets are a generation older. The season's log: 42 of 300 and 16 of 160.

The office is checking the Large Counts condition for a two-proportion z-test before it runs anything. The pooled proportion is $\hat p_c = 0.1261$.

Four products have to clear 10. **What is the smallest of them?** Give it to one decimal place. 
ANSWER: {"type":"numeric","value":20.173913043478258,"kind":"other","digits":1} 
SOLUTION: Under $H_0$ every transit in both groups runs at the pooled rate $\hat p_c = \dfrac{42 + 16}{300 + 160} = 0.1261$.

- hulls carrying the older transponder set · losses expected under H₀: $n 	imes hat p_c = 37.8$
- hulls carrying the older transponder set · non-events expected under H₀: $n(1 - hat p_c) = 262.2$
- hulls carrying the current set · losses expected under H₀: $n 	imes hat p_c = 20.2$
- hulls carrying the current set · non-events expected under H₀: $n(1 - hat p_c) = 139.8$

The smallest is **20.2**, which clears 10.

The condition holds comfortably, so the normal approximation to the sampling distribution of $\hat p_1 - \hat p_2$ is safe to use.

The condition is checked with the **pooled** proportion, not with either sample's own. Using pooled p̂c: n·p = 20.2, n·(1 − p) = 139.8 — both ≥ 10.
===== act-6/what-z-measures 
PROMPT: the Adrastea Lane Office tests $H_0:\ p = 0.176$ against $H_a:\ p > 0.176$ for the proportion of transits that log an off-nominal advisory on the Adrastea feeder lane, and gets $\hat p = 71/330 = 0.215$. Under $H_0$ the standard error of $\hat p$ is $0.0210$, and the standardized test statistic comes out at $1.87$.

What does that number measure? 
ANSWER: {"type":"choice","options":["It is the size of the gap between the sample proportion and the null value: $0.215 - 0.176 = 0.039$, rescaled so that it reads in percentage points.","It measures how far the sample proportion is from the alternative hypothesis, so a large value means $H_a$ fits the data badly.","It is the probability of getting a sample proportion as far from 0.176 as this one, if $H_0$ is true.","It says the observed $\\hat p$ sits about **1.87 standard errors above 0.176**, measured on the sampling distribution $H_0$ predicts — a distance in units of that distribution's own spread."],"correct":3,"feedback":["The raw gap is 0.039, and standardizing does the opposite of what this says — it *removes* the units. Dividing by the standard error 0.0210 leaves a pure number, which is exactly why 1.87 can be compared across corridors and sample sizes with wildly different raw gaps.","The statistic is measured from the **null** value, on the null's own sampling distribution. $H_a$ names no number to measure from. And the sign is the other way round: a large value means the data sit far from $H_0$, which counts *against* the null, not against the alternative.","That is the p-value, and it is a different object: a probability, always between 0 and 1. The standardized statistic 1.87 is a *distance*, and it can be any size at all. The distance comes first; the probability is r 
SOLUTION: $$\text{statistic} = \frac{\hat p - p_0}{\sqrt{p_0(1 - p_0)/n}} = \frac{0.215 - 0.176}{0.0210} = 1.87$$

**It says the observed $\hat p$ sits about **1.87 standard errors above 0.176**, measured on the sampling distribution $H_0$ predicts — a distance in units of that distribution's own spread.**

Every standardized statistic in the course is the same sentence: *(what we saw) − (what the null said) ÷ (how much this statistic ordinarily wobbles)*. The denominator is computed **under $H_0$** — at $p_0 = 0.176$, not at $\hat p = 0.215$ — because the question being asked is what the world would look like if the null were true.

What it buys is comparability. A gap of 0.039 means nothing on its own: on 330 transits it is 1.87 standard errors, and on a tenth as many transits the same gap would be about 0.59 — unremarkable. The raw difference and the sample size only become one number after you divide.
===== act-6/p-from-z 
PROMPT: The Adrastea Lane Office tests the proportion of transits that log an off-nominal advisory on the Adrastea feeder lane against its posted figure of 0.197. The alternative was fixed before the audit and is **one-sided, upper tail**:

$$H_0:\ p = 0.197 \qquad H_a:\ p > 0.197$$

The audit returns 75 off-nominal advisories in 330 transits, so $\hat p = 0.2273$ and the standardized statistic is $z = 1.38$.

What is the p-value? Give it to four decimal places. 
ANSWER: {"type":"numeric","value":0.08338348042752186,"kind":"pValue"} 
SOLUTION: $$z = \frac{\hat p - p_0}{\sqrt{p_0(1 - p_0)/n}} = \frac{0.2273 - 0.197}{0.0219} = 1.38$$

$$P = P\left(Z \ge 1.38\right) = 0.0834$$

**0.0834** — about 1 in 12.

Read it back as a sentence before you move on. *If the proportion of transits that log an off-nominal advisory on the Adrastea feeder lane really were 0.197, an audit of 330 transits would produce a statistic at least this extreme 1 in 12 times.* The shaded area is the upper tail, because that is what the alternative — written before the audit — counts as evidence against $H_0$.
===== act-6/interpret-p-value 
PROMPT: The Adrastea Lane Office tested $H_0:\ p = 0.197$ against $H_a:\ p > 0.197$ for the proportion of transits that log an off-nominal advisory on the Adrastea feeder lane, where $p$ is the true proportion for all transits on the Adrastea feeder lane. The audit of 330 transits returned 75 off-nominal advisories, $\hat p = 0.2273$, $z = 1.38$ and $P = 0.0834$.

**Interpret the p-value in context.** One or two sentences. Do not state a conclusion and do not mention a significance level — this is the p-value and nothing else. 
ANSWER: {"type":"interpretation","required":[{"label":"Conditions on H₀ being true (“assuming H₀ is true…”)","phrasings":[{},{}],"polarity":"any","feedback":"Open with the assumption: “If H₀ is true — the proportion of transits that log an off-nominal advisory on the Adrastea feeder lane is exactly 0.197 — then…”. Without it the sentence is not a p-value at all."},{"label":"“at least as extreme” as the observed statistic","phrasings":[{},{},{},{},{},{}],"polarity":"any","feedback":"The p-value collects every outcome the alternative counts as at least as surprising as the one observed — not just this exact result, whose probability is essentially zero."},{"label":"Calls it a probability (a long-run rate over repeated samples)","phrasings":["probability","proportion of sample","out of"],"polarity":"any","feedback":"Say what 0.0834 is: a probability — equivalently, the fraction of all such samples that would look at least this extreme."},{"label":"Cites the p-value (0.0834)","phrasings":[{}],"polarity":"any","feedback":"Give the number: 0.0834."},{"label":"States the context (what was measured, and for whom)","phrasings":["transit","adrastea","feeder","lane","proportion","log","off","nominal","advisory"],"minMatches":2,"polarity":"any","feedback":"Say what and whom: the proportion of transits that log an off-nominal advisory of transits on the Adrastea feeder lane."}],"forbidden":[{"phras 
SOLUTION: **If H₀ is true — the proportion of transits that log an off-nominal advisory on the Adrastea feeder lane is exactly 0.197 — then a sample of transits on the Adrastea feeder lane would produce a sample proportion at least as extreme as the one observed with probability 0.0834.**

Or, read as a rate: in about 1 in 12 audits of 330 transits drawn from a corridor whose true rate really is 0.197, one would come back at least this far from it.

Four sentences that are **not** this one, and each of them will be quoted back at you by somebody who wants the finding to fail:

- *"There is a 0.0834 probability that $H_0$ is true."* The p-value conditions **on** $H_0$. It is an input to the calculation, not an output of it.
- *"There is a 0.0834 probability the result is due to chance."* Every sample differs from the null by chance. The p-value says how often chance alone would produce a difference at least this large — a statement about the data, not about this corridor.
- *"So there is a 0.9166 probability that $H_a$ is true."* Both numbers are computed entirely under $H_0$; neither is a probability for a hypothesis.
- *"The probability of getting exactly $\hat p = 0.2273$ is 0.0834."* The probability of any exact value is essentially zero. The p-value is a **tail** — the whole set of outcomes at least as extreme.
===== act-6/simulated-p 
PROMPT: The Adrastea Lane Office will not take the normal model on trust, so it simulates instead. It builds 500 seasons of the Adrastea feeder lane in which the proportion of transits that log an off-nominal advisory is held at exactly 0.10 — the posted figure — and counts the off-nominal advisories in 140 transits each time. The table is what came back.

The real season logged **23** off-nominal advisories in 140 transits, and the Office is testing $H_0:\ p = 0.10$ against $H_a:\ p > 0.10$.

What is the **simulated** p-value? Give it to four decimal places. 
ANSWER: {"type":"numeric","value":0.014,"kind":"pValue"} 
SOLUTION: $$P_{\text{sim}} = \frac{\#\{\text{simulated seasons with at least } 23\}}{500} = \frac{7}{500} = 0.0140$$

**0.0140** — 7 of the 500 null seasons were at least as extreme as the one actually logged.

The normal model, run on the same counts, gives $z = 2.54$ and $P = 0.0056$. The two agree to about 0.0084, and they should: the normal curve is a *description* of the distribution the simulation drew from, not a separate claim about it. Where they part company is in the far tail, where 500 runs cannot resolve a probability finer than $1/500 = 0.0020$ — a simulated p-value can never be smaller than one run in 500, however extreme the data are.

The counting rule is the whole lesson. Nothing here needed a curve, a standard error or a table of areas: build the null, run it, and count the runs at least as extreme.
===== act-6/forbidden-sentence 
PROMPT: The Adrastea Lane Office's audit of 330 transits on the Adrastea feeder lane returned 75 off-nominal advisories. Testing $H_0:\ p = 0.197$ against $H_a:\ p > 0.197$ gives $z = 1.38$ and $P = 0.0834$.

Four sentences are drafted for the report. **Exactly one of them would survive being read aloud by somebody who wants the finding to fail.** Which? 
ANSWER: {"type":"choice","options":["The probability of getting exactly $\\hat p = 0.2273$ from 330 transits is 0.0834.","If the true proportion of transits that log an off-nominal advisory on the Adrastea feeder lane were exactly 0.197, then about 0.0834 of all audits of 330 transits would return a sample proportion at least as far above 0.197 as this one did.","There is a 0.0834 probability that $H_0$ is true — that the proportion of transits that log an off-nominal advisory on the Adrastea feeder lane really is 0.197.","Since $P = 0.0834$, there is a 0.9166 probability that $H_a$ is true and the true rate really is above 0.197."],"correct":1,"feedback":["**A point, not a tail.** The p-value collects every outcome the alternative counts as at least as extreme — that whole shaded region, not the single value the audit landed on. (The probability of any one exact sample proportion is far smaller than 0.0834.)",null,"**The probability that H₀ is true.** The p-value is computed by *assuming* $H_0$; it is an input to the arithmetic, not a verdict on it. $H_0$ is either true of the Adrastea feeder lane or it is not, and a sample cannot turn that into a probability.","**1 − p as the probability of Hₐ.** Both 0.0834 and 0.9166 are computed entirely inside the world where $H_0$ holds. Neither is a probability that any hypothesis is true; the complement of a conditional probability is still co 
SOLUTION: **If the true proportion of transits that log an off-nominal advisory on the Adrastea feeder lane were exactly 0.197, then about 0.0834 of all audits of 330 transits would return a sample proportion at least as far above 0.197 as this one did.**

Every defensible reading of a p-value has the same skeleton: *assuming $H_0$* — *the probability* — *of a statistic at least as extreme as the observed one* — *in context*. Drop the first clause and the sentence becomes a claim about the hypothesis; drop the third and it becomes a claim about one impossible-to-repeat outcome.

- **The probability that H₀ is true.** The p-value is computed by *assuming* $H_0$; it is an input to the arithmetic, not a verdict on it. $H_0$ is either true of the Adrastea feeder lane or it is not, and a sample cannot turn that into a probability.
- **"Due to chance."** Under $H_0$ the difference is due to chance *with certainty* — chance is the only mechanism in the model. What 0.0834 measures is how often chance alone would produce a difference at least this large, which is a statement about the data, not about the cause.
- **1 − p as the probability of Hₐ.** Both 0.0834 and 0.9166 are computed entirely inside the world where $H_0$ holds. Neither is a probability that any hypothesis is true; the complement of a conditional probability is still conditional on the same thing.
- **A point, not a tail.** The p-value collects every outcome the alternative counts as at least as extreme — that whole shaded region, not the single value the audit landed on. (The probability of any one exact sample proportion is far smaller than 0.0834.)

This matters more than a vocabulary drill usually does, because the wrong sentences are not wrong by a little. Each of them makes a claim the arithmetic cannot support, and each is the first thing a hostile reader will take apart.
===== act-6/one-vs-two-tails 
PROMPT: The Adrastea Lane Office's audit of 420 transits on the Adrastea feeder lane returned 46 off-nominal advisories against a posted figure of 0.097, giving $z = 0.87$. Written as a one-sided test — $H_a:\ p > 0.097$ — the p-value is 0.1929.

A reviewer objects that the Office had no advance reason to pick a side, and that the alternative should have been **two-sided**: $H_a:\ p \ne 0.097$. On the **same** statistic $z = 0.87$, what is the two-sided p-value? Give it to four decimal places. 
ANSWER: {"type":"numeric","value":0.3858192139029836,"kind":"pValue"} 
SOLUTION: $$P_{\text{one-sided}} = P(Z \ge 0.87) = 0.1929 \qquad P_{\text{two-sided}} = 2 \times P(Z \ge 0.87) = 0.3858$$

**0.3858** (the other reading being 0.1929).

The factor of two is not a correction, a penalty or an adjustment for anything the data did. It is the whole content of the alternative: a two-sided $H_a$ says a departure in *either* direction would count, so the tail on the far side has to be counted too, and the curve's symmetry makes it the same size.

Which is why the alternative is written before the data. If the Office picks the side after seeing which way the audit fell, it will reject on either tail while quoting the smaller number — a procedure whose real false-alarm rate is twice the one printed on the page.
===== act-6/p-value-shading 
PROMPT: The Adrastea Lane Office tested $H_0:\ p = 0.167$ for the proportion of transits that log an off-nominal advisory on the Adrastea feeder lane and got a standardized statistic of $z = 2.81$. The display is the sampling distribution of $z$ **under $H_0$** — the standard normal curve — with the region the office shaded.

Which alternative hypothesis does that shading correspond to, and what is the p-value it represents? 
ANSWER: {"type":"display","display":{"kind":"normal","mean":0,"sd":1,"shade":{"from":2.81,"to":4}},"question":{"type":"choice","options":["$H_a:\\ p < 0.167$ — the alternative points less than the null value — and the p-value is $0.0025$.","$H_a:\\ p > 0.167$ — a one-sided alternative — and the shaded area is the p-value, $0.0025$.","$H_a:\\ p \\ne 0.167$ — a two-sided alternative — and the shaded area is the p-value, $0.0050$.","$H_a:\\ p > 0.167$, and the p-value is the **unshaded** area, $0.9975$ — the probability that the null is not contradicted."],"correct":1,"feedback":["The shading is on the upper side of the curve, so the outcomes being counted as extreme are the ones greater than the null value. An alternative pointing less than would have shaded the other tail, and on this statistic it would have returned 0.9975 — almost all of the curve.",null,"A two-sided alternative shades **both** tails, and the picture shows one. The value quoted here, 0.0050, is what you would get after adding the mirror tail beyond $z = −2.81$ — but that region is not shaded, so it is not what this display shows.","The p-value is the area the alternative counts as \"at least as extreme\", which is the shaded tail beyond the statistic — 0.0025. The unshaded 0.9975 is everything *less* extreme, and it is not a probability about the null at all."]}} 
SOLUTION: **$H_a:\ p > 0.167$ — a one-sided alternative — and the shaded area is the p-value, $0.0025$.**

The curve is the distribution of the standardized statistic *if $H_0$ were true*, so every point on it is a season that the null could produce on its own. The alternative decides which of those seasons count as being at least as extreme as the one observed, and the p-value is their total probability.

One tail, upper: $P(Z \ge 2.81) = 0.0025$ — the shaded region and nothing else.

The same statistic read two-sided would be 0.0050, because it would add the mirror tail beyond $z = −2.81$ — an area the display does not shade. Doubling a one-tailed area is a decision about the *alternative*, made before the data; it is never something you read off a picture of one tail.
POOLED 0.011868300153139357